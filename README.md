## Installation
1. Clone the repository
2. Install the required packages by running `uv sync`
3. Install the package locally by running `uv pip install -e .`

## Example usage
For an example of how to generate explanations for a qhole dataset see `scripts/explain_bcos_gnn.py`.
